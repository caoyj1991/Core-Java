apply plugin: 'java'

sourceCompatibility = 1.8


repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile group: 'com.jcraft', name: 'jsch', version: '0.1.54'
    compile fileTree(dir: 'vendor-lib', include: '*.jar')
}